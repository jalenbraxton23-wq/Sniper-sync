name = "sniper-sync"
main = "sync-worker.js"
compatibility_date = "2026-08-01"

# After creating your D1 database, bind it here (or via the dashboard's
# Worker Settings -> Bindings -> Add D1 Database instead -- either works,
# the dashboard way doesn't require redeploying).
# [[d1_databases]]
# binding = "DB"
# database_name = "sniper-sync"
# database_id = "your-database-id-here"
